# Progress Tracker

Update this file after every completed feature.
Any AI agent reading this must immediately know what is done, what is in progress, and what is next.

---

## Current Status

**Phase:** Phase 6 — Production (dark theme conversion arc Sessions 16–19 **COMPLETE**)
**Last completed:** Session 23 — Three Generate page features. (1) **Prospect name extraction**: `buildPrompt` updated to explicitly scan for founder/CEO/named contact; falls back to "Hi [Company] team". (2) **Subject line generator**: `buildPrompt` now returns structured `SUBJECT_LINES:/EMAIL:` format; new exported `parsePitchResponse` in `lib/gemini.ts`; route parses and saves only `emailContent` to DB, appends `__METADATA__` trailer with subject lines; PitchForm handles trailer + post-loop split-chunk recovery; PitchOutput renders subject lines section with per-line copy buttons (Check icon uses `text-success-foreground`). (3) **Tone badge**: `"{tone} · {wordCount} words"` pill in PitchOutput header. Review found and fixed: demo route regression (now calls `parsePitchResponse`), regex robustness (`\n+EMAIL:\n`), split-chunk sentinels (post-loop fullText checks), insert-failure UX (non-fatal — user retains email), token violation (`text-green-500` → `text-success-foreground`), stale copiedIndex (`key={line}` + useEffect reset).
**Next:** Phase 6, Feature 16: RLS policies audit (the queue item deferred at the start of the dark theme arc).

---

## Progress

### Phase 1 — Foundation
- [x] 01 Project scaffold
- [x] 02 Supabase setup
- [x] 03 Auth (login page visually redesigned in Session 10 — see Decisions Made; auth logic itself unchanged)

### Phase 2 — Core Pages Shell
- [x] 04 Root layout + Navbar
- [x] 05 Homepage (full redesign in Session 8 — see Decisions Made)
- [x] 06 Profile page

### Phase 3 — Core Feature
- [x] 07 Website fetcher
- [x] 08 Gemini integration
- [x] 09 Generate page (visually redesigned + usage tracking added in Session 11 — see Decisions Made; core generation logic unchanged)

### Phase 4 — History
- [x] 10 Pitch history

### Phase 5 — Polish
- [x] 11 Incomplete profile banner
- [x] 12 Empty states
- [x] 13 Loading states
- [x] 14 Error handling

### Phase 6 — Production
- [ ] 15 Environment variable audit
- [ ] 16 RLS policies
- [ ] 17 Deploy to Vercel

---

## Decisions Made During Build

- `create-next-app@latest` installs Next.js 16 by default. Pinned `package.json` to `next@^15` / `eslint-config-next@^15` (installed: 15.5.19) to match architecture.md.
- shadcn/ui init and `lib/utils.ts` (`cn()`) deferred to Phase 2 — not needed until the first UI primitive is built.
- Root project intentionally left outside git for now (create-next-app's nested git init was discarded during scaffold merge).
- Features 02 + 03 built together in one session — auth can't be verified without the DB existing, and the DB wiring can't be verified without auth working. `profiles`/`pitches` tables and RLS were applied manually in the Supabase dashboard beforehand (not via this session).
- Built minimal stub pages at `app/(app)/generate`, `/history`, `/profile` (+ pass-through `app/(app)/layout.tsx`, no Navbar yet) purely so the post-login middleware redirect could be verified in a browser. Real content for these routes lands in Features 06/09/10 — only the page body gets replaced, not the route.
- `signOut` server action lives in `actions/auth.ts` (not explicitly listed in architecture.md's tree, but follows its `actions/` convention). Temporarily wired to a plain button on the `/generate` stub via `<form action={signOut}>` until Feature 04 moves it into the real Navbar. Had to type `signOut` as `Promise<void>` (no `{success, error}` return) because Next.js form actions require a void-compatible return signature — returning an error object on the failure path broke the type contract with `<form action>`.
- Google OAuth required two manual dashboard steps outside the codebase: enabling the Google provider in Supabase (Authentication → Providers) and registering the exact Supabase callback URL (`https://<project-ref>.supabase.co/auth/v1/callback`) as an authorized redirect URI in Google Cloud Console. Both are one-time project config, not something future sessions need to repeat.
- `@supabase/supabase-js` logs a build warning about a Node.js API not supported in the Edge Runtime (used by `@supabase/ssr` inside `middleware.ts`). This is a known, harmless warning — build and runtime both work correctly despite it.
- Feature 04 (Navbar): the sign-out form moved from the `/generate` stub into `components/layout/Navbar.tsx`. Navbar is a Client Component (`usePathname` for active-link state); `app/(app)/layout.tsx` stays a Server Component and just renders `<Navbar />` above `{children}`, per code-standards.md's "never `use client` in layout files" rule. The `?error=signout_failed` banner stays on `/generate` since `signOut` always redirects failures there regardless of originating page.
- Feature 05 (Homepage): `app/page.tsx` checks auth server-side (same pattern as `/login`) and redirects logged-in users to `/generate` before rendering anything. Hero and "how it works" sections are inline in the page file (no extracted components, page-specific only) — reuses the existing Card token set for the 3 step cards. New `ui-registry.md` entries: Hero Section, Step Card.
- Feature 06 (Profile page): first form in the app. `app/(app)/profile/page.tsx` (Server Component) fetches the user's `profiles` row server-side and passes it as `initialData` into `components/profile/ProfileForm.tsx` (Client Component) — the client form never touches the DB directly. `actions/profile.ts`'s `saveProfile` **upserts** on `user_id` (`onConflict: "user_id"`) since there's no DB trigger creating a profile row on signup — this means the same action works for both brand-new and returning users without a separate "create profile" step. Tone field uses a plain native `<select>` styled with the same token classes as text inputs, not a shadcn primitive — first time the "approved dependencies" check (shadcn? native Next.js? simpler native solution?) favored the native solution. Validation in `saveProfile` is manual (`if` checks), not zod — zod stays uninstalled; first real validation need that arises with more complex input (e.g. Feature 09's generate action) should revisit whether zod is worth pulling in then. Added a new `types/index.ts` file with `Profile` and `Tone` types — first file in `types/`.
- Features 07 + 08 + 09 built together in one session (per `/architect`) since the fetcher and Gemini integration have no way to verify they work without the generate page wiring them together. `npm install cheerio @google/generative-ai` — first installs since project scaffold. `lib/fetcher.ts`'s `fetchWebsiteText` normalizes the URL (prepends `https://` if no scheme), uses a 10s `AbortController` timeout + a real `User-Agent` header (some sites block the default fetch UA), and rejects non-2xx/non-`text/html` responses before handing HTML to cheerio — none of this HTTP-level robustness was in library-docs.md's cheerio snippet, which only covered the parsing half. `lib/gemini.ts`'s prompt explicitly bans bracket placeholders (`[Company Name]`, `[Your Name]`, etc.) and instructs Gemini to use the real company name from the site content, falling back to the URL's domain (via `lib/fetcher.ts`'s `extractDomain`) if the name can't be determined. `app/api/generate/route.ts` does its own `auth.getUser()` check and 401s if missing — `middleware.ts`'s matcher checks `pathname.startsWith("/generate")`, which `"/api/generate"` does not satisfy, so API routes are **not** covered by the existing middleware protection. Regenerate re-runs the entire pipeline (re-fetch + re-call Gemini) through the exact same endpoint and request body as the initial generate — every call, including regenerates, inserts a new immutable `pitches` row; there is no "skip fetch, reuse content" mode. `prospect_summary` is the first ~300 chars of the cleaned site text (word-boundary trimmed), not a second Gemini call — architecture.md describes `lib/gemini.ts` as writing the email only. `PitchOutput` is rendered conditionally inside `PitchForm` (which owns all url/result/loading/error state) rather than introducing a third wrapper component, mirroring the Feature 06 server-page + single-client-component pattern. Validation stayed manual (no zod) — one URL field still isn't complex enough to justify it; revisit if a future feature needs more than that.
- First live test of Feature 09 failed with a 502 (`[api/generate] gemini failed`) on every request — root cause was `"gemini-1.5-flash"` itself, not the integration code: Google retired the entire Gemini 1.5 line, so every `generateContent` call 404'd. Confirmed via a standalone script hitting `ListModels` directly. Switched `lib/gemini.ts` to `"gemini-2.5-flash"` (current stable fast model) and verified a real call succeeds. `library-docs.md` updated with the new model name and a note to re-run `ListModels` if this happens again — its model recommendations can go stale since they're a point-in-time snapshot, not a live source of truth.
- `/review` on Features 07–09 found 6 issues, all fixed: (1) `PitchForm.tsx`'s Regenerate now re-submits `pitch.prospect_url` instead of the live URL input — previously, editing the input after a successful generate without resubmitting would silently regenerate a different prospect under the "Regenerate" label. (2) The URL input is now `disabled` while a request is in flight. (3) `lib/fetcher.ts` had no SSRF protection — `fetchWebsiteText` now resolves the hostname via `dns.lookup` and rejects loopback/private/link-local addresses (covers cloud metadata endpoints), and switched to `redirect: "manual"` so a 3xx response can't bypass the host check; no new dependency needed, Node's built-in `dns` module is enough. (4) `app/api/generate/route.ts`'s `const body = await req.json()` was an implicit `any` — added a `parseUrl(body: unknown)` helper that narrows properly, one justified `as { url: unknown }` cast with a comment per code-standards.md's exception clause. (5) `architecture.md`'s boundary table said AI calls live "in `app/api/generate/route.ts` only" and listed `lib` under "Never in" — contradicted its own Folder Structure section, which defines `lib/gemini.ts` as the Gemini wrapper. Reworded to `lib/gemini.ts` + API route, matching the `lib/fetcher.ts` row's phrasing. (6) `app/(app)/generate/page.tsx` now does its own `auth.getUser()` + redirect, matching the Profile/Homepage pattern, instead of relying solely on `middleware.ts`.
- Separately tuned `lib/gemini.ts`'s prompt only (no other files touched, by request): emails are now capped at 80 words and structured as exactly three sentences (specific observation about the prospect → connects it to the sender's services → call to action) plus a sign-off, with an explicit instruction not to spend more than one sentence on the sender's own background.
- Feature 10 (Pitch history) built per `/architect`. `ui-rules.md`'s Do Nots gained one exception (`position: fixed` permitted for modal overlays only) to unblock a real modal — first conflict between an existing rule and a new feature's needs. Modal + delete-confirmation both use shadcn/ui Dialog, first shadcn install in the project: `npx shadcn init` was far more invasive than expected, overwriting `app/layout.tsx` (Inter → Geist) and injecting a competing oklch theme into `app/globals.css` — both restored, and the generated `components/ui/dialog.tsx` was fully rewritten to use only this project's tokens (no shadcn default classes remain). Correction for the record: this shadcn version builds Dialog on `@base-ui/react`, **not Radix** — `code-standards.md`'s Approved Dependencies list updated accordingly (`@base-ui/react`, `clsx`, `tailwind-merge`, `tw-animate-css`; `shadcn` itself moved to devDependencies as a codegen-only tool). Unused `components/ui/button.tsx` and `class-variance-authority` (its only consumer) were removed since `DialogFooter` takes plain buttons from each caller instead of a shared `<Button>`. `actions/pitches.ts`'s `deletePitch` enforces ownership at the DB level (`.eq("id", id).eq("user_id", user.id)` in the delete query itself, not a fetch-then-check) per explicit instruction. `/review` found 4 issues, all fixed: an unjustified `as Pitch[]` cast in `app/(app)/history/page.tsx` (removed — `tsc` stayed clean without it); a keyboard-only bug where Enter/Space on the row's Delete button bubbled to the row's own `onKeyDown` and opened the detail dialog too (fixed with `stopPropagation` on the button's `onKeyDown`, mirroring its `onClick`); a silent error/empty-state conflation where a failed Supabase fetch rendered the cheerful "no pitches yet" empty state instead of an error banner (fixed); and the detail dialog rendering an empty `<p>` for falsy `email_content` instead of following Pitch Output's fail-safe-empty precedent (fixed with a muted fallback message — the row itself stays visible/deletable, unlike Pitch Output which returns `null` entirely, since this is saved history rather than an in-flight result). New `ui-registry.md` entries: Dialog (Modal Overlay & Content Shell), Pitch History Row, Pitch History List.

---

- Features 11–14 planned together via `/architect` (the project-local skill folder exists but isn't surfaced by the harness's skill registry — invoked manually by reading `skills/architect/SKILL.md` directly) and built in one session, bundled with an out-of-plan visual-polish pass toward a Linear/Resend/Vercel-style light SaaS look. Feature 11 (incomplete profile banner): `app/(app)/generate/page.tsx` runs a second inline `.select("services")` query (matching every other page's own-query pattern, no shared helper) and shows a new warning-token banner (first real use of the warning tokens beyond the Direct tone badge) when `!profile || !profile.services?.trim()`. Feature 12 (empty state): turned out to require a structural change beyond "add a placeholder" — `PitchOutput`'s `pitch` prop became `Pitch | null` and it now always mounts (via `PitchForm`) instead of being conditionally rendered; the component itself branches internally between a new Sparkles-icon empty state and the real textarea/buttons, which **replaces** the old "return `null` for empty content" precedent from Feature 10. Feature 13 (loading states): `Loader2 animate-spin` added alongside (not replacing) the existing status text on Generate/Regenerate/Save, plus one shared `app/(app)/loading.tsx` covering all three authenticated routes (`min-h-screen` centered spinner, no skeletons, per ui-rules.md). Feature 14 (error boundaries): added root-level `app/error.tsx` (client boundary, reuses the Auth Card shell, "Try again" calls `reset()`) and `app/not-found.tsx` (same shell, link to `/generate`) — no nested per-segment boundaries. The `/history` error-handling audit this feature called for found nothing to fix: it already logs `console.error("[app/history]", error)` and renders a distinct error banner instead of falling through to the empty state.
- Visual polish (not in build-plan.md, added by explicit request this session): Navbar swapped `bg-surface` for `backdrop-blur-md bg-white/80` (frosted glass over scrolled content). Homepage hero headline got a gradient-text treatment (`bg-gradient-to-r from-accent to-accent-hover bg-clip-text text-transparent`) plus two blurred background orbs — the orbs required the hero `<section>` to be `relative overflow-hidden` (containment on narrow screens) with the actual heading/subhead/CTA wrapped in their own `relative z-10` div, because plain static content paints *before* `z-index: 0` positioned siblings in CSS stacking order and would otherwise render underneath the orbs, not above them. Card hover lift (`hover:shadow-md hover:border-border-strong transition-all duration-200`) was deliberately scoped to cards in an interactive flow or repeated grid — Step Cards and the Pitch Output card — and explicitly excluded from one-off static shells (Auth Card, Profile Form's wrapping card); each interactive card carries an inline "interactive card — hover lift intentional" comment. The Generate button alone got an indigo glow-on-hover (`hover:shadow-[0_0_20px_rgba(91,78,232,0.4)]`) — not applied to Regenerate or any other Primary button. A full `transition-colors` audit (`grep -rn "transition-colors" | grep -v duration`) found and fixed three places missing an explicit duration (`Navbar.tsx`'s nav links and sign-out button, `PitchHistoryRow.tsx`'s row and Delete button, `dialog.tsx`'s close button) — all now `duration-150`, matching the app-wide standard. Copy button's "Copied!" label also changed to "Copied ✓" per this session's explicit request.

- Session 8 — Homepage redesign (out-of-plan visual overhaul, added by explicit request): `app/page.tsx` rebuilt from scratch as a full 5-section landing page. Hero became a two-column grid with a static browser window mock on the right, a 4-layer background depth system (gradient wash + 3 orbs + bottom fade — all sanctioned exceptions per ui-rules.md's "no gradients on card surfaces" rule), eyebrow badge, pill CTA buttons (`rounded-full h-12 text-base px-7` — marketing-only, never in the app interior), and social proof row. Stats Bar used the `gap-px bg-border` grid divider trick. How It Works moved to a semantic `<ol>` grid with icon chips, uppercase step labels, hover translate lift, and ArrowRight connector divs placed as siblings to the `<ol>` (not children — invalid HTML). CTA card uses `rounded-3xl` (marketing-only, first/only use in the project). Footer adds brand block + 4-column link grid + bottom bar. Tailwind built-in colors (`via-purple-500`, `to-violet-400`, `bg-rose-400`, `bg-amber-400`, etc.) used as design-spec-explicit exceptions for gradient stops and decorative dots — not hex values, not violating the no-hex rule. `Link` lucide icon aliased as `LinkIcon` to avoid collision with Next.js's `Link`. `/review` found one issue (Link2 vs Link icon) — fixed immediately. `ui-registry.md` updated with 2 revised entries and 6 new entries (18 total): Hero Section, Step Card, Stats Bar, CTA Section, Browser Window Mock, Footer, Pill Button, Eyebrow Badge.

- Session 9 — Homepage hero polish + animated browser demo (out-of-plan, added by explicit request): three carry-over visual fixes from Session 8 applied first — hero H1 gradient span needed `inline-block` (bare `<span>` doesn't reliably clip `background-clip: text`), CTA card padding reduced `py-16 sm:py-20` → `py-12 sm:py-16`, Stats Bar wrapper margin `-mt-6` → `mt-0` to sit flush against the hero instead of overlapping it. Then the static browser window mock was extracted from `app/page.tsx` into a new Client Component, `components/marketing/BrowserDemo.tsx`, animating a 5-phase loop (idle → typing → analyzing → writing → done) entirely client-side — `app/page.tsx` itself stays a Server Component. Animation is one root `useEffect` running an `async` loop awaiting a `setTimeout`-backed `wait()` helper (not nested literal timeout chains), with a `cancelled` flag plus `clearTimeout` cleanup guaranteeing no leaked timers and no post-unmount `setState`. `prefers-reduced-motion` is checked once via `matchMedia` and short-circuits straight to the static done state, never starting the loop. A `cursor-blink` keyframe was added to `globals.css` (sharp step, not `animate-pulse`) for the typing-phase cursor. No new npm dependencies. `/review` flagged one spec ambiguity (Phase 4 "writing" button color — interpreted as `bg-accent-light` + `text-accent`, carried over from analyzing) and one minor structural note (redundant per-dot `aria-hidden` removed in favor of one root-level `aria-hidden="true"`) — both accepted by the developer. `ui-registry.md`'s Browser Window Mock entry updated in place (renamed "(animated)", file path and table updated, new orchestration pattern documented) rather than duplicated.

- Session 10 — Login page redesign (out-of-plan visual overhaul, added by explicit request): `app/(auth)/login/page.tsx` rebuilt around a new "Login Card" pattern, distinct from the project's existing Auth Card shell — two decorative `-z-10` background orbs (accent-toned, `blur-3xl`, same sanctioned-exception justification as the homepage orbs), a `size-12 rounded-xl bg-accent` logo mark with a white Zap icon, new heading/subtext copy, the existing page-level error banner repositioned (`mt-4`), a wrapped Google sign-in button, and a new trust line. Auth logic (`createClient()`/`getUser()`/`redirect`, `searchParams.error`) preserved exactly. `components/auth/GoogleSignInButton.tsx` had only its `<button>` element restyled — new bordered/light-hover treatment plus an inline official 4-color Google "G" SVG — all hooks, the OAuth click handler, the internal `hasError` banner, and the loading-text swap left untouched; the existing `disabled:opacity-50 disabled:cursor-not-allowed` treatment was deliberately kept even though the new spec's classlist omitted it. The Google SVG's hardcoded brand hex fills (`#4285F4`/`#34A853`/`#FBBC05`/`#EA4335`) are the project's one sanctioned hex exception — fixed third-party brand colors, not part of this app's token palette. `ui-registry.md` gained a new "Login Card" entry and the existing "Auth Card" entry's notes were corrected to say it's now only used by `app/error.tsx`/`app/not-found.tsx`, not login. Manual browser verification (new design, OAuth flow, error banner, and specifically whether the static Google icon reads naturally next to "Redirecting…" text during loading) is still outstanding, same as Session 9's BrowserDemo animation checks — both are queued for the developer's next browser pass.

- Session 11 — Generate page redesign (out-of-plan visual overhaul + usage-tracking groundwork, added by explicit request): DB migration added `plan`/`pitches_this_month` to `profiles`. New `UsageInfo` type kept deliberately separate from the shared `Profile` type (billing fields never bloat the type used by `ProfileForm`/`saveProfile`/the Gemini prompt). `app/(app)/generate/page.tsx` rebuilt into a two-column layout (orbs, `grid grid-cols-1 lg:grid-cols-5`). `components/pitch/PitchForm.tsx` now returns a Fragment with two grid-item divs (`lg:col-span-2`/`lg:col-span-3`) so one Client Component populates both halves of the parent grid — this is now the established pattern for any future component needing the same trick. Added a usage-quota progress bar (first documented inline-`style` exception in the project, for the runtime-variable width), a Profile Summary Card mutually exclusive with the existing incomplete-profile banner, and a limit-reached `Link`-to-`/pricing` branch wrapping (not duplicating) the existing Generate button logic. `components/pitch/PitchOutput.tsx` restyled into a header/divider/body shell with Ghost-style Copy/Regenerate buttons. An initial pass mistakenly replaced the editable textarea with static `<p>` paragraphs per a literal spec reading — caught in `/review` and reverted; the body is a borderless/transparent `<textarea>` styled to look like static paragraphs, and this is now the canonical "editable but looks static" pattern for the project. `pitches_this_month` increments via a non-atomic read-then-write in `app/api/generate/route.ts` after each successful insert (acceptable since there's no limit enforcement yet — pure preparation for future billing). `ui-registry.md` updated: Pitch Form, Pitch Output revised; new Usage Indicator, Profile Summary Card, Generate Page Shell entries added. **Unresolved:** Generate's new heading style (`font-semibold tracking-tight`, no `mb-6`) deviates from the documented "Page Heading (interior pages)" pattern (`font-bold`, `mb-6`, used by `/profile`) — flagged in the registry, not yet resolved either direction.

- Session 12 — Demo page feature (`/demo`, new out-of-plan feature, added by explicit request): new `demo_usage` table (`ip_address`, `cookie_id`, unique index on the pair, RLS enabled with zero policies so only a service-role client can touch it). New `lib/supabase-admin.ts` (service-role client, bypasses RLS, first use of `@supabase/supabase-js` directly rather than the `@supabase/ssr` cookie-based wrapper) and `lib/demo.ts` (shared cookie-name constant + IP-extraction helper, avoiding three divergent copies across middleware/page/route). `middleware.ts` gained a cookie-ensure block for `/demo`/`/api/demo-generate` only (never added to `protectedRoutes`) — a real bug was caught and fixed during the build: the demo cookie's `Set-Cookie` call had to be deferred until after the Supabase client's own cookie-sync callback runs, since that callback unconditionally reassigns the response object and would otherwise silently drop the demo cookie. New `app/api/demo-generate/route.ts` is fully separate from `/api/generate` (which was not modified at all this session) — reuses `lib/fetcher.ts`/`lib/gemini.ts` directly with a generic built-in profile, checks `demo_usage` by `(ip_address, cookie_id)` before generating, and only inserts a usage row on successful generation (a failed attempt never consumes the one free try). New `app/demo/page.tsx` (Server Component, public, outside `(app)`) and `components/demo/DemoForm.tsx` (Client Component, deliberately isolated from `PitchForm`/`PitchOutput` — intentional visual-class duplication, not drift). `ui-registry.md` gained a new Demo Form entry. Migration run and `SUPABASE_SERVICE_ROLE_KEY` added to `.env.local` by the developer, dev server restarted — feature should be functionally testable. **Unresolved:** manual browser verification of the full demo flow (incognito, second-attempt block, different-cookie-same-IP behavior, real `/generate` unaffected) has not been done yet.

- Small fix (between Sessions 12 and 13, logged here since it wasn't captured at the time): the homepage CTA section's "Book a demo" button was a dead `<button>` with no `href`/`onClick`, left over from before `/demo` existed. Relabeled "Try the demo" and wired as `Link href="/demo"`. The hero's "See how it works" button has the same problem and is still unwired — not fixed, flagged for a future session.

- Session 13 — Stripe billing foundation, Session A of 2 (out-of-plan, added by explicit request; zero Stripe SDK calls this session, that's Session B): DB migration upgraded `profiles.plan` from a 2-value to a 3-value check constraint (`free`/`standard`/`pro`) and added `stripe_customer_id`, `stripe_subscription_id`, `subscription_status` columns plus a partial unique index on the customer ID. `UsageInfo` extended with the three Stripe fields; `lib/utils.ts` gained `PLAN_LIMITS = { free: 5, standard: 50, pro: Infinity }` as the single source of truth, replacing the hardcoded `FREE_PITCH_LIMIT = 5` in `PitchForm.tsx`. Usage-indicator copy now adapts per plan — drops "free" wording on paid tiers (two separate spots needed this fix, the second caught during `/review`), and the entire usage-quota block is omitted entirely for Pro (`Infinity` limit) rather than showing a meaningless "X of Infinity" or even an "Unlimited" label. New public `app/pricing/page.tsx`: three-tier cards (Free $0, Standard $10/mo "Most popular", Pro $25/mo) reusing the Generate Page Shell's orb background; first "featured/emphasized card" pattern in the project (`border-2 border-accent` + larger padding + absolute "Most popular" pill). `app/(app)/layout.tsx` became `async` for the first time — does its own fetch-only `getUser()` + `profiles.plan` query (no redirect logic, doesn't duplicate the protective checks elsewhere) and passes `plan` down to `Navbar` as a new prop; this is now the established pattern for any future "shared layout needs user-specific data" case. `Navbar.tsx` gained a 4th nav link (Pricing) and a plan badge. "Current plan" button logic on the pricing cards was generalized to all three tiers (not just Free, as the literal spec described) — confirmed acceptable, currently unreachable for Standard/Pro until Session B's checkout exists. `ui-registry.md` gained a new Pricing Card entry; Navbar entry updated for the badge/link/new data-flow.

- Session 14 — Stripe billing, Session B of 2 (out-of-plan, added by explicit request; completes the billing work started in Session 13 — real test-mode Checkout, webhooks, Customer Portal): `npm install stripe`, added to `code-standards.md`'s approved dependencies. New `lib/stripe.ts` (SDK instance with `apiVersion` read directly from the installed package's bundled type defs rather than guessed, plus `PLAN_TO_PRICE_ID`/`planForPriceId()` as the single source of truth for both directions of the plan↔price-ID mapping). New `app/api/checkout/route.ts` (authenticated, user-scoped client, reuses or creates `stripe_customer_id`, creates a subscription-mode Checkout Session). New `app/api/webhooks/stripe/route.ts` — reads raw body via `req.text()` (never `.json()`), verifies the signature via `stripe.webhooks.constructEvent` before trusting any payload data, handles `checkout.session.completed`/`customer.subscription.updated`/`customer.subscription.deleted` via the **service-role** admin client (the only place besides the demo feature that touches it), wraps event handling in its own try/catch separate from signature verification so handler errors are logged but the route still always returns 200 except on signature failure. New `app/api/billing-portal/route.ts` (authenticated, user-scoped client). New `components/billing/UpgradeButton.tsx` and `ManagePortalButton.tsx` — same fetch→redirect-or-error shape as `GoogleSignInButton`/`DemoForm`, redirect via `window.location.href` since the destination is Stripe-hosted, deliberately kept as two separate small components rather than one shared abstraction. `app/pricing/page.tsx`'s disabled "Coming soon" buttons replaced with real `UpgradeButton`s — the free-card logic needed zero changes. `app/(app)/profile/page.tsx` gained a new Billing card (matches the page's existing `rounded-xl` card tier, not the marketing `rounded-2xl` tier). `app/(app)/generate/page.tsx` gained a `?upgraded=true` success banner. One unjustified type cast (`card.id as "standard" | "pro"`) was caught and removed during the build itself — TypeScript already narrows that value correctly through the existing ternary chain. The two `as Stripe.X` casts inside the webhook route are necessary and commented (Stripe's own `Event.data.object` type isn't discriminated by `event.type`). `ui-registry.md` gained Upgrade Button / Manage Portal Button and Billing Section entries; Pricing Card entry updated. **Critical unresolved item:** the full Stripe billing feature (Sessions A + B combined) is code-complete and type-checks cleanly but has **not been manually verified end-to-end** — no real test-mode Checkout has been run yet, and the webhook round-trip is entirely unconfirmed. This is the highest-priority verification item across the whole project right now.

- Session 15 — One-time cleanup script + canceling-state feature (out-of-plan, added by explicit request): **Part 1**, `scripts/cleanup-duplicate-subs.ts` (gitignored, never committed, fully deleted after use) listed and canceled accumulated test-mode Stripe subscriptions — 7 found, 6 canceled, 1 kept. Required an async-IIFE workaround since `tsx` compiles `.ts` as CommonJS by default in this repo (no `"type": "module"`), which doesn't support top-level `await`. **Part 2**, real feature: `profiles.cancel_at timestamptz` column; `handleSubscriptionUpdated` in the webhook route syncs it; `handleSubscriptionDeleted` also resets it to null (a gap not in the original spec, caught and fixed during the build). New shared `formatDate()` helper in `lib/utils.ts`. UI: Navbar plan badge gets a `bg-warning-light` "Canceling" variant, Profile page gets a cancellation-date message, `PitchForm` gets a renewal banner above the usage indicator. **This session involved an extended live-debugging investigation before the real fix was found** — full details in `memory.md`, but the headline finding: a webhook event's actual JSON shape is governed by whichever API version Stripe rendered that specific event under (visible in `stripe listen`'s own startup banner), which can differ from the version pinned in `lib/stripe.ts`. This caused `subscription.cancel_at_period_end` (boolean) to be unreliable for Portal-driven cancellations in this environment — confirmed empirically via temporary diagnostic logging against the Stripe Dashboard as ground truth. The fix uses `subscription.cancel_at` (a separate, directly-populated timestamp field) instead. **Bonus fix:** `app/api/checkout/route.ts` previously had no check for an existing active subscription, so repeated "Upgrade" clicks created duplicate parallel subscriptions (this is what caused the 7 found in Part 1) — fixed by updating the existing subscription's price in place via `stripe.subscriptions.update()` with proration when one already exists, falling back to a fresh Checkout Session only for genuinely new subscribers. `ui-registry.md` updated: Navbar (canceling badge variant), Billing Section (cancellation line), Pitch Form (renewal banner). **Stripe billing (Sessions 13-15) is now manually verified end-to-end by the developer** — the first Stripe work in this project to reach that bar, not just type-check cleanly.

- Session 19 — Dark theme conversion, Session 4 (FINAL) (2026-07-01): History page (PitchHistoryList converted to Client Component — real-time search filter + animated pitch count badge Overshoot spring + staggered row entrance + hover lift y:-2). Pricing page (new PricingCards.tsx Client Component — Pro card premium treatment: bg-accent-light, "Best value" badge, pulsing glow blob in inner overflow-hidden wrapper, outer glow shadow; Standard restored to normal border; stagger 0.1s; hover lift y:-4 Standard, y:-6 Pro; UpgradeButton already had motion from Session 18). Demo page (orbs removed; DemoForm panel slide-in + shadow corrected + input bg-background added). loading.tsx (use client + fade-in). error.tsx + not-found.tsx (stagger entrance). ManagePortalButton (spring scale). /review found 1 bug (self-caught before agent returned): Pro card overflow-hidden was clipping the -top-3 badge — fixed by moving overflow-hidden to an inner wrapper containing only the blob. **Zero rgba(91,78,232,...) old-accent values remain in the codebase** (confirmed via grep). ui-registry.md stale entries updated: CTA Section, Pill Button, Pricing Card, Upgrade/Manage Button entries all updated to reflect dark theme values. Token Baseline "not yet audited" list replaced with a completion summary.

- Session 18 — Dark theme conversion, Session 3 of N (2026-07-01): Generate page (orbs removed; PitchForm left/right column panel slide-in via `panelTransition` from lib/motionVariants.ts; Generate button shadow corrected `rgba(91→108,...)`; button now `motion.button` with spring hover/tap + loading shimmer overlay via `SHIMMER_X` stable constant; service badge stagger via new `badgeContainer`/`badgeItem` exports; all gated by `useLazyReducedMotion()`). PitchOutput: email reveal animation keyed on `pitch.id` (re-animates per generation), copy button hover glow. Profile page: extracted `ProfileLayout.tsx` Client Component wrapping both cards with panel slide-in; ProfileForm gets field stagger + save button spring+ripple (monotonic-key counter prevents same-pixel ripple collision). UpgradeButton gets motion + Upgrade shadow. TypeScript clean. `/review` found and fixed 3 bugs: Generate button missing `flex items-center justify-center` on the button element (not the inner span), shimmer array new-reference on every render (hoisted to constant), ripple key collision on same-pixel rapid click (monotonic ref). Also fixed: PANEL_TRANSITION was duplicated between PitchForm and ProfileLayout (consolidated into lib/motionVariants.ts), Ripple type was between Props and component (reordered). 1 old-accent remaining in DemoForm.tsx, deferred. Manual verification handed off (OAuth-gated routes).

- Session 17 — Dark theme conversion, Session 2 of N (2026-07-01): Login page (Glass Card, stagger entrance with `staggerContainer`/`staggerItem` from new `lib/motionVariants.ts`, orb drift motion via `LoginOrbs.tsx`, pulsing logo boxShadow, Google button hover spring + white glow) + Homepage (H1/H2 gradient migrated to new 2-token `from-accent-gradient-start to-accent-gradient-end` utilities; hero left column extracted to `HeroContent.tsx` with stagger entrance; step cards to `StepCards.tsx` with `whileInView` stagger + framer-motion hover lift replacing CSS translate; CTA section to `CTAContent.tsx` with blob pulsing + button springs; `BrowserDemo.tsx` migrated to Glass Card). Hero background orbs recolored to canonical Orb spec values using existing token utilities. All old-accent `rgba(91,78,232,...)` eliminated from touched files; 2 remaining in `DemoForm.tsx`/`PitchForm.tsx` deferred. `/review` (4 of 8 angles completed, 3 hit Sonnet session limit) found 2 real issues both fixed: 7 copies of the reduced-motion check consolidated into `lib/useLazyReducedMotion.ts`, 3 copies of stagger variant constants consolidated into `lib/motionVariants.ts`. TypeScript and ESLint both clean. Manual verification handed off to developer (public routes, no OAuth required — check at localhost:3001).

- Session 16 — Dark theme conversion, Session 1 of N (out-of-plan, added by explicit request; interrupts the Phase 6 RLS-audit queue): scoped to exactly three pieces per the developer's instructions — token swap, dialog scrim fix, Navbar rebuild. `app/globals.css`'s entire `@theme` block replaced with `ui-tokens.md`'s dark values verbatim, including 4 new gradient tokens (`--color-accent-gradient-start/end`, `--color-accent-gradient-btn-start/end`) and one token not in the original spec: `--color-scrim: rgba(0, 0, 0, 0.6)`, added as a deliberate choice over a raw `bg-black/60` utility (this project's `ui-tokens.md` is strict about tokens-everywhere; a modal scrim is reusable enough to deserve one) — documented in a new "Modal Scrim" section of `ui-tokens.md`. `components/ui/dialog.tsx`'s `DialogOverlay` swapped `bg-text-primary/20` → `bg-scrim`; the old class silently inverted meaning once `--color-text-primary` flipped from near-black to white (was a correct dark tint on a light backdrop, became a wrong translucent white wash on a dark one). `components/layout/Navbar.tsx` fully rebuilt: frosted `backdrop-blur-md bg-white/80` → opaque `bg-surface border-b border-border`; logo "P" `text-white` → `text-accent-foreground`; the old per-link className ternary for active state replaced with a Framer Motion `layoutId="navPill"` shared-element pill (active link text stays `text-text-primary`, the pill carries `bg-accent-light` — this required correcting a stale `ui-tokens.md` Typography row that still said active nav text should be `text-accent`); `motion.header` slide-down on mount (`y: -100 → 0`); logo gets a spring `whileHover` bounce; all gated on `prefers-reduced-motion`, detected via a **lazy `useState` initializer** rather than `BrowserDemo.tsx`'s post-mount `useEffect` pattern — a deliberate, documented divergence (`ui-rules.md`'s Motion Patterns gained a note explaining when each timing applies), not drift, since framer-motion only honors a `motion` component's `initial` prop on its very first commit and a `useEffect`-based check would fire one render too late to skip the entrance animation. `npm install framer-motion` (already present in `code-standards.md`'s approved list from a prior uncommitted edit). `/review`'s 8-angle pass found and fixed 3 real issues: a Navbar code comment falsely claiming `dialog.tsx` shared the same reduced-motion handling (it has none — rewritten to explain the real `initial`-prop-timing reasoning instead); `transition-colors duration-150` present on both active/inactive nav-link text branches before the rewrite, dropped to inactive-only in the first pass (restored on both); the stale `ui-tokens.md` Typography row mentioned above. One `/review` suggestion was investigated and explicitly rejected: switching to framer-motion's built-in `useReducedMotion()` hook — reading its actual source in `node_modules` showed it has the identical one-time-read limitation as the hand-rolled version (the library's own source carries a `// TODO See if people miss automatically updating` comment) plus an unwanted dev-mode console warning, so the hand-rolled version was kept. `/imprint` updated the Navbar and Dialog `ui-registry.md` entries in place and added a new "Token Baseline — Established 2026-06-30" section capturing the full dark `@theme` block as the registry's color source of truth, explicitly flagging ~20 other component entries (Login Card, Pricing Card, Pill Button, etc.) whose documented `rgba()`/hex glow values are still tuned for the old light-theme accent and have not yet been re-validated against the new one — out of scope this session. **Manual browser verification could not be completed by Claude**: `/generate`/`/history`/`/profile` (where Navbar and the dialogs actually render) sit behind Google OAuth via `middleware.ts`, which can't be driven headlessly in this environment, and `chromium-cli` wasn't available; the developer took over manual verification with a specific handoff checklist (navbar slide-on-load, pill sliding cleanly between Generate/History/Profile/Pricing, logo hover bounce, dialog scrim darkness, reduced-motion behavior via DevTools emulation) — not yet confirmed back as of this update. Also worth noting: this repo has only one git commit ever (`167768c "app"`); 15+ sessions of work sit uncommitted in the working tree, which meant `/review`'s diff had to be manually scoped to just this session's edits rather than relying on a raw `git diff HEAD`.

- Session 23 — Three Generate page features (prospect name extraction, subject line generator, tone badge): `buildPrompt` updated to (a) explicitly scan website content for founder/CEO/co-founder/director names and use first name in greeting, and (b) return structured `SUBJECT_LINES:\n1...\nEMAIL:\n` format. New `parsePitchResponse(raw)` exported from `lib/gemini.ts` — uses `\n+EMAIL:\n` (not literal single newline) to handle blank lines LLMs commonly emit after numbered lists. `app/api/generate/route.ts`: after stream completes, calls `parsePitchResponse` → saves only `emailContent` to pitches table; insert failure is now non-fatal (logs + continues) so user retains their email even if DB save fails; `__METADATA__${JSON.stringify({subjectLines})}` enqueued as the absolute last chunk. `app/api/demo-generate/route.ts` fixed: imports and calls `parsePitchResponse` before returning JSON — the shared `buildPrompt` change would have returned SUBJECT_LINES markers as email_content without this. `components/pitch/PitchForm.tsx`: `subjectLines` state (cleared at start of each generation); per-chunk `__METADATA__` handling; post-loop split-chunk recovery (checks assembled `fullText` for both sentinels, handles the case where proxies/edge runtimes split a single server enqueue across multiple reads); post-loop `EMAIL:\n` parse for clean email display; passes `subjectLines` + `tone` to PitchOutput. `components/pitch/PitchOutput.tsx`: new `subjectLines` + `tone` props; subject lines section (key={line}, not index; `copiedIndex` state reset by useEffect on subjectLines change; Check icon uses `text-success-foreground` not `text-green-500`); tone badge `"{tone} · {wordCount} words"`. Subject lines are ephemeral — never saved to DB, never in Pitch type, never shown in History.

- Session 22 — Real-time streaming generation (out-of-plan architectural change, added by explicit request): `lib/gemini.ts` gained `generatePitchStream(websiteContent, profile, domain)` — same signature/buildPrompt as `generatePitch`, uses `model.generateContentStream` instead of `model.generateContent`, returns an `AsyncGenerator<string>` via an inner `async function* streamText()`. `generatePitch` is byte-identical; the demo route (`app/api/demo-generate/route.ts`) uses it unchanged. `app/api/generate/route.ts` restructured: no longer has a single outer try/catch — each failure mode is handled explicitly (`req.json()` wrapped, fetch wrapped, stream-init wrapped, stream errors caught inside `ReadableStream.start`). New: `plan` column added to the profile query; server-side `isPlan`/`PLAN_LIMITS` quota check returns 403 before any generation starts (client-side `limitReached` guard was previously the only enforcement). The response is now `new Response(stream, { "Content-Type": "text/plain; charset=utf-8" })` — not `NextResponse.json`. Inside `ReadableStream.start`: chunks are enqueued as they arrive; after the generator is exhausted, `fullText = chunks.join("")` → Supabase insert → usage increment → `controller.close()`. `__STREAM_ERROR__` is enqueued on any error inside the stream, then the stream closes. `components/pitch/PitchForm.tsx`: `GenerateResponse` union type removed. `generate()` now: clears pitch to null, checks `response.ok` (JSON-parses error if false), reads body via `getReader()` loop, checks for `__STREAM_ERROR__` **before** appending chunk to `fullText` (throws if found, caught by outer catch → error banner), calls `setPitch(prev => ...)` on each chunk to update the displayed email in real time (pitch id stays `"streaming"` throughout streaming — framer-motion re-animation only fires once when `hasPitch` first becomes true), calls `router.refresh()` after loop exits, `setIsLoading(false)` in `finally`. `handleSubmit` and `handleRegenerate` unchanged — both delegate to `generate()`. Existing `Pitch` type unchanged: `id: "streaming"` is a valid string satisfying the type during in-flight state; after `router.refresh()`, history shows the real persisted pitch from the DB.

## Notes

_Add notes here as the build progresses — workarounds, patterns, anything that differs from the context files._
